name: Build (stable)

#on:
#  push:
#    branches: ["stable"]
#    paths-ignore:
#      - ".github/**"
#      - "*.md"
#  workflow_dispatch:

on:
  push:
    tags:
      - "stable-v*"
  workflow_dispatch:

jobs:
  build:
    uses: JappeOS/jappeos-workflows/.github/workflows/build-package.yml@main
    with:
      package-name: jappeos-terminal
      flutter: true
      channel: stable
    secrets: inherit